# liquid.yaml

# Parameters
nc: 3  # Number of classes
names: ['empty', 'half', 'full']  # Class names

# Paths for training and validation datasets
train: /Users/richtsai1103/liquid_level_drone/dataset/trainval_dataset/images/train/
val: /Users/richtsai1103/liquid_level_drone/dataset/trainval_dataset/images/val

# Number of channels, for RGB images it's 3
channels: 3

# python3 yolov5/train.py --img 640 --batch 16 --epochs 100 --data liquid.yaml --cfg yolov5/models/yolov5s.yaml
